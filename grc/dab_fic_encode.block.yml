# auto-generated by grc.converter

id: dab_fic_encode
label: 'DAB: FIC encoder'
category: '[DAB]'

parameters:
-   id: dab_mode
    label: DAB Mode
    dtype: int
    default: '1'
    options: ['1', '2', '3', '4']
    option_labels: [Mode 1, Mode 2, Mode 3, Mode 4]
-   id: samp_rate
    label: Sampling Rate
    dtype: int
    default: samp_rate

inputs:
-   domain: stream
    dtype: byte

outputs:
-   domain: stream
    dtype: byte

templates:
    imports: from gnuradio import dab
    make: dab.fic_encode(dab.parameters.dab_parameters(mode=${dab_mode}, sample_rate=${samp_rate},
        verbose=False))

documentation: |-
    Encodes the FIC.

file_format: 1
