# auto-generated by grc.converter

id: dab_fib_source_b
label: 'DAB: FIB source'
category: '[DAB]'

parameters:
-   id: transmission_mode
    label: Mode
    dtype: int
    options: ['1', '2', '3', '4']
    option_labels: [Mode 1, Mode 2, Mode 3, Mode 4]
-   id: coutry_ID
    label: Country ID
    dtype: int
-   id: num_subch
    label: Number of sub-channels
    dtype: int
-   id: ensemble_label
    label: Ensemble label
    dtype: string
-   id: programme_service_labels
    label: Programme service labels
    dtype: string
-   id: service_comp_lang
    label: Service language
    dtype: raw
-   id: protection_mode
    label: Protection modes
    dtype: raw
-   id: data_rate_n
    label: Data rates / 8kbit/s
    dtype: raw
-   id: dabplus
    label: Dabplus
    dtype: raw

outputs:
-   domain: stream
    dtype: byte

templates:
    imports: from gnuradio import dab
    make: dab.fib_source_b(${transmission_mode}, ${coutry_ID}, ${num_subch}, ${ensemble_label},
        ${programme_service_labels}, "", ${service_comp_lang}, ${protection_mode},
        ${data_rate_n}, ${dabplus})

file_format: 1
