# auto-generated by grc.converter

id: dab_mp4_encode_sb
label: 'DAB: MP4 Encoder'
category: '[DAB]'

parameters:
-   id: bit_rate_n
    label: Bitrate / 8kbit/s
    dtype: int
-   id: channels
    label: Channels
    dtype: int
    options: ['1', '2']
    option_labels: [Mono, Stereo]
    hide: part
-   id: samp_rate
    label: Audio Sample Rate
    dtype: int
-   id: afterburner
    label: Afterburner
    dtype: int
    options: ['1', '0']
    option_labels: [Enable, Disable]

inputs:
-   label: PCM
    domain: stream
    dtype: short
    multiplicity: ${ channels }

outputs:
-   label: mp4
    domain: stream
    dtype: byte

templates:
    imports: from gnuradio import dab
    make: dab.mp4_encode_sb(${bit_rate_n}, ${channels}, ${samp_rate}, ${afterburner})

file_format: 1
