# auto-generated by grc.converter

id: reed_solomon_encode_bb
label: 'DAB: RS Encoder'
category: '[DAB]'

parameters:
-   id: bit_rate_n
    label: bit rate / 8 kbit/s
    dtype: int

inputs:
-   domain: stream
    dtype: byte

outputs:
-   domain: stream
    dtype: byte

templates:
    imports: from gnuradio import dab
    make: dab.reed_solomon_encode_bb(${bit_rate_n})

file_format: 1
