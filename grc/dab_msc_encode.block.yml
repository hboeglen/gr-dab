# auto-generated by grc.converter

id: dab_msc_encode
label: 'DAB: MSC encoder'
category: '[DAB]'

parameters:
-   id: dab_mode
    label: DAB Mode
    dtype: int
    default: '1'
    options: ['1', '2', '3', '4']
    option_labels: [Mode 1, Mode 2, Mode 3, Mode 4]
-   id: samp_rate
    label: Sampling Rate
    dtype: int
    default: samp_rate
-   id: data_rate_n
    label: Data rate / 8 kbit/s
    dtype: int
-   id: protection
    label: Protection Mode
    dtype: int
    options: ['0', '1', '2', '3']
    option_labels: [A1, A2, A3, A4]

inputs:
-   label: mp4_audio
    domain: stream
    dtype: byte

outputs:
-   domain: stream
    dtype: byte

templates:
    imports: from gnuradio import dab
    make: dab.msc_encode(dab.parameters.dab_parameters(mode=${dab_mode}, sample_rate=${samp_rate},
        verbose=False), ${data_rate_n}, ${protection})

file_format: 1
